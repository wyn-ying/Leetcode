###idea
此题求反码的思路很明确，找到和num位数一样且每一位都是1的数，进行抑或运算。问题的关键在于确定最高位的位置的位数。
第一个办法是```max=1```之后每次左移一位，直到```num<max```，此时```max-1```就是与num相同位数且每一位都是1的数。
第二个办法是max每左移一位，都在最后一位填上1，这样直到```num<=max```时，max和num位数相同。

###solutions
同第一个办法

###time consuming
#####my solution
43ms-->beats 58.52%